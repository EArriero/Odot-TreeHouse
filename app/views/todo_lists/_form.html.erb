<!-- # Comment. builder: FoundationFormBuilder is a key/value pair that would use the FoundationFormBuilder. It is in the /lib. -->
<%= form_for(@todo_list, builder: FoundationFormBuilder) do |f| %>
  <%= f.text_field :title, label: "List Title", autofocus: "autofocus" %>
  
  <%= f.submit "Save" %>
<% end %>